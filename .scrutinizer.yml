build:
    environment:
        python: 3.6.3
    dependencies:
        override:
            - pip install pipenv
            - pipenv install --dev --skip-lock
    tests:
        override:
            - true
